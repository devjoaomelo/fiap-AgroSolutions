apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-property
  namespace: agrosolutions
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-property
  template:
    metadata:
      labels:
        app: postgres-property
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: PROPERTY_DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: PROPERTY_DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: agrosolutions-secrets
              key: PROPERTY_DB_PASSWORD
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-property-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-property
  namespace: agrosolutions
spec:
  selector:
    app: postgres-property
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-property-pvc
  namespace: agrosolutions
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi