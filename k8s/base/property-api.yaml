apiVersion: apps/v1
kind: Deployment
metadata:
  name: property-api
  namespace: agrosolutions
spec:
  replicas: 2
  selector:
    matchLabels:
      app: property-api
  template:
    metadata:
      labels:
        app: property-api
    spec:
      containers:
      - name: property-api
        image: devjoaomelo/agrosolutions-property:latest
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ConnectionStrings__DefaultConnection
          value: "Host=$(PROPERTY_DB_HOST);Port=5432;Database=$(PROPERTY_DB_NAME);Username=$(PROPERTY_DB_USER);Password=$(PROPERTY_DB_PASSWORD)"
        - name: PROPERTY_DB_HOST
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: PROPERTY_DB_HOST
        - name: PROPERTY_DB_NAME
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: PROPERTY_DB_NAME
        - name: PROPERTY_DB_USER
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: PROPERTY_DB_USER
        - name: PROPERTY_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: agrosolutions-secrets
              key: PROPERTY_DB_PASSWORD
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: property-api-service
  namespace: agrosolutions
spec:
  selector:
    app: property-api
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP